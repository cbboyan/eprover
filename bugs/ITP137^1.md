# ITP137^1 — Second trigger for bug002

See `bugs/bug002-eta-self-rewrite.md` for full root cause, fix, and verification.

## Difference from ITP035^1

ITP137^1 triggers the same assertion (`term!=replace` in `TermAddRWLink`) via different
call paths — `term_is_top_rewritable` (lines ~235, ~267) and `term_find_rw_clauses`
(line ~1087) — which the initial bug002 fix did not cover.

The key flag is `--prefer-initial-clauses`, which changes clause processing order and
causes the backward rewriting paths to be hit before the forward rewriting path.

## Minimal reproduction

```sh
eprover-ho --cpu-limit=1 --prefer-initial-clauses ITP137^1.p
```

---

## Minimization findings

### The crashing demodulator

All four crashing instances in `term_find_rw_clauses` use the same demodulator `i_0_484`
(an eta-reduction rule for `produc811245928lle_fm`, the pair case-split combinator):

```
produc811245928lle_fm @ (^[Z0,Z1]: X370 @ Z0 @ Z1) @ X169
= produc811245928lle_fm @ X370 @ X169
```

### Why it self-rewrites

When the term being rewritten is already of the form
`produc811245928lle_fm @ (^[Z0,Z1]: F @ Z0 @ Z1) @ X`,
the demodulator matches with `X370 ↦ (^[Z0,Z1]: F @ Z0 @ Z1)`. The RHS is then
`produc811245928lle_fm @ (^[Z0,Z1]: F @ Z0 @ Z1) @ X`, which after `MakeRewrittenTerm`
beta/eta-normalizes back to the original term. Hence `repl == term` → crash.

### The four crashing terms

All four have the shape:
```
produc811245928lle_fm @ (^[Z0,Z1]: <applied-var> @ Z0 @ Z1) @ (produc1000205111lle_fm @ X)
```
with different applied-variable bodies (the lambda body is exactly what the demodulator LHS expects).

### Likely axiom origin

`i_0_484` is likely derived from `fact_182_case__prod__eta` (axiom in ITP137^1.p):
```
produc811245928lle_fm @ (^[Z0,Z1]: X449 @ (produc1943344577lle_tv @ Z0 @ Z1)) = X449
```
combined with pair-constructor unfolding, yielding the eta rule with identity lambda.

### Next step for full minimization

Trace `i_0_484` back through parent clauses in `-l4` output to find the exact axiom set,
then construct a minimal TPTP problem with fresh short type names.
